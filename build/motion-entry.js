// Source for js/vendor/motion.js. Rebuild with: npm run build:motion
// Only the DOM helpers are pulled in, so no React ends up in the bundle.
export { animate, inView, stagger, hover } from "framer-motion/dom";
